# 2026.247.1546 — chore: align the pod-selkies pin to v2026.246.2210

## Summary

Aligns this repo's `pod-selkies` pin to **v2026.246.2210** (the newest version, what the resolver already picks) — one of the multi-version warning sources (alongside layer-wl-screenshot-pixelflux, pod-kde-selkies and the distro closure) that blocked the charly cachyos-import re-vendor.

## How tested

```
$ charly box validate   # repo pod-kde-selkies
box validate: OK — checked 17 candies, 7 distros, 7 builders; 0 warnings, 0 errors
```

The full downstream graph gate (the cachyos import) re-runs at the re-vendor PR once all four sources align; this PR proves its own pin resolves at 0/0. This is one of several coordinated pin alignments (recorded in plan/crabbox-loop-cutover.md), not a standalone claim of graph-wide clearance.

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow + layer skills loaded before the pin change. |
| R1 — RCA every anomaly | The warning was traced to this stale pin; fixed at the source. |
| R2 — finish the whole cutover | Pin aligns to the merged newest tag. |
| R3 — no duplication | One canonical newest version. |
| R4 — no workarounds | Pin to the merged tag; no suppressions. |
| R5 — delete legacy | N/A. |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | `box validate` 0/0 at the fresh binary. |
| R8 — preserve emitted artifacts | Manifest validates. |
| R9 — binary equals source | Fresh worktree-built bin/charly. |
| R10 — fresh disposable proof | N/A — pin-only (resolution validated at 0/0; the downstream graph gate is the re-vendor PR). |
| RDD/ADE/SDD | N/A. |
| Concurrency / Disposable / Hooks | N/A. |

## Change classification

- **Class:** dependency pin alignment (merged tag).
- **Verification gate:** `charly box validate` (CI + fresh binary), 0/0.
- **Attribution:** analysed on a live system (pin-only; downstream gate at the re-vendor PR).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*